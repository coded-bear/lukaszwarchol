export interface ButtonProps {
  onClick: any;
  active: boolean;
  name: string;
}

export interface ProjectProps {
  projectName: string;
  image: string;
  lang: string;
}

export interface ProjectSingleProps {
  lang: string;
}
